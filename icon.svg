<svg width="512" height="512" viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg">
  <!-- Background -->
  <rect width="512" height="512" rx="100" fill="#00B4A6"/>

  <!-- Two Overlapping Circles (centered and scaled) -->
  <g transform="translate(156, 156)">
    <!-- Teal Circle (left) - white on teal background -->
    <circle cx="80" cy="100" r="90" fill="none" stroke="#FFFFFF" stroke-width="20"/>
    <!-- Amber Circle (right) -->
    <circle cx="160" cy="100" r="90" fill="none" stroke="#F4A443" stroke-width="20"/>
  </g>
</svg>
