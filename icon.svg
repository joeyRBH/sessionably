<svg width="512" height="512" viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg">
  <!-- Background -->
  <rect width="512" height="512" rx="100" fill="#00B4A6"/>

  <!-- Two overlapping circles (centered) -->
  <g transform="translate(130, 180)">
    <!-- Teal circle (left) - White on teal background -->
    <circle cx="100" cy="76" r="70" fill="none" stroke="#FFFFFF" stroke-width="16"/>
    <!-- Amber circle (right) -->
    <circle cx="152" cy="76" r="70" fill="none" stroke="#F4A443" stroke-width="16"/>
  </g>
</svg>
